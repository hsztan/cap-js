export const getShows = async () => {
  try {
    //TODO
    const showsIds = [];
    const fetches = [];
    // Resolve all fetches
    const data = await response.json();
    console.log(data);
  } catch (error) {
    console.log(error.message);
  }
  return null;
};
